# Spectrum最终工件独立核验

2026-09-21。本次在[取回完成标记](D:/graduate/第二阶段T-SAC实施记录/Spectrum最终工件取回校验.json)发布后，只读核验gpu2关闭run及最终15景validation工件；未SSH、训练、模型推理或修改原件。

**结论：本轮核验全部通过，未发现阻塞交付的问题；已知失败归档P2本次未触发，但仍未代码修复。** 逐项证据保存于[Spectrum最终工件独立核验.json](D:/graduate/第二阶段T-SAC实施记录/Spectrum最终工件独立核验.json)。

## 预算与恢复谱系

新attempt `20260921T053400_b66b2ea983ba`保留原trial `1184e3c111924c619a74252587b30f53`，父run为`20260921T031515_931ec8ab244a`，父检查点为`180cc4eb1a4d44768287ec6201bedfeb`。原`initialization`在监督来源、运行清单及训练结果中一致，热身身份仍为`initialized_policy`。

计数从150回合/21294环境步/20295更新恢复至**300回合/42632环境步/41633更新**，本attempt新增150回合、21338环境步及21338更新。训练耗时**4250.802101秒 < 5940秒**；加上原先保守计入的4860秒，共**9110.802101秒 < 10800秒**。4860秒是既有预算核查的保守估计，不冒称检查点保存的精确累计秒数。

监督结果为completed，训练退出码0，errors及warnings为空；初查中的作业步骤`206357.1`为`COMPLETED / 0:0`。本核查据这些已取回记录核对，没有再次登陆节点查询进程。

## 两个关闭数据库

两份SQLite均以只读、immutable方式打开，`integrity_check=ok`、运行状态completed、ERROR事件数为0。

- 训练库：恰好**150个完成的train回合**；21338条转移的累计环境步连续覆盖21295至42632，最终更新计数41633。最终检查点的持久事件水位不超过数据库已保存事件尾部。
- 最终评价库：恰好**15个完成的validation回合及15个唯一场景**，实际检查点ID及训练计数与最终sidecar一致，无test回合混入本次评价。

这两库只覆盖本次gpu2 attempt及其最终评价。原gpu4前150回合的训练SQLite未取得，**不能把累计300回合计数写成本地300回合训练SQLite全部齐备**。

## 检查点、发布工件与物理复算

本地唯一最终权重为`8c0ac11408ba47faa271c402dc94cdec/检查点.pt`，独立重算SHA256为：

`15f1b9ab7b15af270023270e58eeef2825f5a2b0fddba1cdf78cba439f83d883`

文件、sidecar、run/trial身份及300回合计数一致。数据库中的**217条本地可及发布工件**逐项SHA通过；另14条未取回的发布权重均命中声明的远端保留清单，不计为缺失错误。外层取回范围共声明19个排除项：15个pt（14个中间权重和1个最新权重别名）继续保留home，另4个是未取的临时SQLite WAL/SHM（两个WAL均为0字节）；本地不是全模型归档。整包257个源文件的回传SHA核对已由主线程记录，本轮没有重复冒称执行了另一遍整包校验。

最终**15个NPZ、225个数组**均以`allow_pickle=False`读取；字段、形状、类型、有限值、连续频谱块、功率守恒及约束校验通过。逐景源CSV字节SHA与冻结validation记录一致，交接中的几何、需求、group、增益、噪声和服务顺序均与真实源场景相同。

从交接数组重建完整账本并独立重算物理，**15/15通过、零违规**；速率、SINR、槽功率及平均满足度与导出值和评价报告一致。复算15景平均U为**0.8373774648846322**。未使用缓存历史速率替代此次重算，也未读取test用于选参。

本次只核验工件与执行闭合，不重新评价其他模型、重做已通过的seed42共同250回合工件，亦不据单个续训流声称正式多seed显著性或未知root来源独立。最终权重仅做字节SHA及sidecar身份核验，未再次加载全部优化器/Replay开展恢复演练。
