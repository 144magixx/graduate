# gpu2 恢复前结果核查

2026-09-21，只读本地[持久状态盘点](D:/graduate/第二阶段T-SAC实施记录/gpu2恢复前持久状态盘点.json)。盘点内监督进度更新时间为 **12:35:33 北京时间**；它是恢复前快照，不能把其中旧 `running` 状态当作目前仍在 gpu4 运行。本次没有 SSH、训练、改参数或改状态 JSON。

## 首个 50 回合配对已齐

从原始逐场景行独立重算均值与 2000 次场景配对 bootstrap（随机种子 0），与快照保存值精确一致。差值均为 Residual−CNN：

- **seed42**：两组均新增 50 回合、7128 环境步、6129 次更新。CNN U **0.8766088470**；Residual U **0.8819079782**；差 **+0.0052991311**，95% 区间 **[−0.0072603534, +0.0159287591]**，跨 0。
- **seed43**：两组均新增 50 回合、7006 环境步、6007 次更新。CNN U **0.8578150611**；Residual U **0.8858064018**；差 **+0.0279913408**，95% 区间 **[+0.0073332197, +0.0507387711]**，未跨 0。

两组 seed 内的检查点计数一致，全部 15 个 validation 场景的 ID、实体/正需求数、需求总量、6000W 预算及 phase 相同，全部零硬约束违约。区间是场景抽样区间，**不是训练 seed 区间**；两种续训随机流共享同一预训练父模型，不能据此宣布从零独立多 seed 优势或最终性能已提升。

## 最终原始值与共同预算

已有三个终点评价，均完整 15 景、零违约：

- CNN42：新增 **300 回合 / 42721 步 / 41722 更新**，U **0.8685360020**。
- Residual42：达到墙钟上限后在完整回合边界结束，新增 **271 回合 / 38629 步 / 37630 更新**，用时 **10836.05 秒**，U **0.8196817866**。
- CNN43：新增 **300 回合 / 42632 步 / 41633 更新**，U **0.7759684908**。

Residual43 尚无最终评价。三个原始均值已从逐场景结果核同；与四个首检查点评价合计七份评价使用完全相同的 15 景及需求/预算口径。105 条结果是重复评价同一组场景，不能当作 105 个独立场景。

seed42 原始终点差约 **−0.0488542154**，但预算为 **300 对 271**，快照已正确标记 `unmatched_budget`，不能将该差当作等预算算法比较，也没有可用的最终配对 CI。两侧持久检查点的**最大共同预算是 250 回合 / 35619 环境步 / 34620 更新**：

- CNN42，run `20260921T004424_cf74dbeb8d38`，checkpoint `2dbbe6f5d1be4b80b840e3f1a922414a`；清单 SHA256 `12f3be72a361d6ed1c46d338fc5b382ddc810ebe993c5639a77a543bf41e4500`。
- Residual42，run `20260921T004424_7256a95e074e`，checkpoint `0982a14d555d42ffa83e8fad740e543c`；清单 SHA256 `2623ac681825fe86808b956a8489e73b511499dadef8dc6a384823dd7edc5f3a`。

本快照两侧 `matched_evaluation` 均为空，**250 回合的共同预算评价尚无结果，不能用终点值代填**。seed42 已履行原墙钟预算，不建议补训 29 回合。

## seed43 的可恢复边界

Residual43 持久副本只含 50、100、150 回合检查点，最新可恢复点为 **150 回合 / 21294 步 / 20295 更新**：run `20260921T031515_931ec8ab244a`，checkpoint `180cc4eb1a4d44768287ec6201bedfeb`，清单 SHA256 `37a368f168fc7c06f234ec987069a8106e29f46abf55b7ee0c42ea891d108bf5`。

监督快照显示的 160 个已完成回合及后续活动步不构成新的持久检查点，不能据其声称从 160 精确恢复。应按用户本轮恢复授权，从持久 150 边界继续；本核查没有执行恢复。上述 SHA 为盘点记录的 sidecar 声明，本次未另行下载或重算远端权重内容 hash。
