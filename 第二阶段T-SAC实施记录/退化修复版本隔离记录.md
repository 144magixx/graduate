# 退化修复版本隔离记录

日期：2026-09-21。本文件保留脚手架交接时点的历史状态；后续修复、GPU验收与继续训练见《Spectrum修复验收与代码审查报告.md》。

脚手架时点状态：Spectrum T-SAC 独立脚手架已建立并完成基础检查，尚未实施模型结构修复、启动训练或操作远端。

## 隔离范围

源链路为 `PPO4090/implementations/horizon_tsac_20260920/`，独立副本为 `PPO4090/implementations/spectrum_tsac_20260921/`。复制源码、配置与运行依赖共 53 个文件，排除 `__pycache__`、字节码和临时文件。原 22 个测试文件复制至 `PPO4090/tests/spectrum_tsac_20260921/`，只替换包前缀和路径常量。

新实现替换完整包前缀、`HORIZON_*` 路径常量及算法显示名；训练、监督、独立评价、导出、看板、验证和打包均使用新包。模型层、算法公式、默认编码器、超参数及既有语义版本字段保持继承，后续模型修改及相应版本更新由修复阶段单独完成。监督器内部默认算法选择标识仍沿用 `horizon`，但实际启动模块已经是 Spectrum；它不是历史源码导入。

`project_paths.py` 新增：

- `SPECTRUM_IMPLEMENTATION = "spectrum_tsac_20260921"`。
- `SPECTRUM_OUTPUT_DIR = OUTPUTS_DIR / SPECTRUM_IMPLEMENTATION`，产物与 Horizon 完全分目录。
- `SPECTRUM_DATA_DIR = HORIZON_DATA_DIR`，共用已冻结数据。
- `SPECTRUM_FRONTEND_DIR = HORIZON_FRONTEND_DIR`，共用只读 UI。
- `SPECTRUM_REPORT_DIR = HORIZON_REPORT_DIR`，实施文档统一归档。

根 `AGENTS.md` 和 `PPO4090/项目说明.md` 已追加 Spectrum 路由，不替换历史说明。新的默认打包清单、静态验收输出名称加 Spectrum 标识，以免覆盖既有 Horizon 同名验收文件。

## Horizon 保留证据

复制前冻结原实现 53 文件及原测试 22 文件，共 75 个文件的 SHA256，保存于 [Horizon 源码冻结清单](./退化修复Horizon源码冻结清单.json)。复制后、基础检查后和打包检查后再次核验，75 个文件全部一致；旧源码、配置和测试未修改。缓存不属于该源码冻结清单。

完整逐文件哈希、基础检查结果和打包文件列表见 [版本隔离验收](./退化修复版本隔离验收.json)。其中的新文件哈希仅标识本次脚手架交接时点；后续获授权的 Spectrum 修复应生成自己的最终验收记录，不覆盖原 Horizon 冻结证据。

## 基础检查

- 全项目 AST 语法检查：168 个 Python 文件通过。
- 新包非训练安全导入：42 个模块通过；没有导入两个 `train.py` 入口或执行 `__main__.py`。
- 新实现及新测试没有残留旧完整包前缀或 `HORIZON_*` 路径常量。
- 输出根不同，数据及只读前端别名符合隔离约定。
- 真实生成并读取临时基础验收运行包：195 个文件，包含新实现、新测试、冻结数据、兼容 CSV、共享前端 `src` / `dist` 与依赖清单；不含 Horizon 实现/测试、输出历史、缓存或 `node_modules`。历史 `sac_fyh_io` 源码仍作为只读兼容审计依赖携带。

临时运行包保留在 `D:/graduate/.tmp-spectrum-isolation/Spectrum脚手架打包验收.tar.gz`，仅验证打包白名单。后续模型修改后必须重新打包，不得将该基础副本作为修复完成版本部署。

本步骤按范围没有运行完整测试集合、训练、评价或远程操作，也未读取旧 test 场景/评分来选参。基础检查不代表模型退化已修复或新模型性能已验证。

## 后续交接

修复工作从 `implementations/spectrum_tsac_20260921/models/networks.py` 等新目录推进；模型结构、默认配置及语义版本更新均属于后续阶段。完成后运行新测试集合和独立短验，再生成具名新运行包；历史 Horizon 目录与已完成实验继续保持只读。
