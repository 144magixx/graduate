# PPO确定性解码独立诊断

日期：2026-09-21。**本次未复现违反冻结协议的工程bug；实际权重确认了“ALLOC总概率很高，但单个SKIP成为联合概率最大动作”的解码效应。不能把低分直接写成训练采样策略整体塌缩。** 原validation U=0.00446565703787455保留，不改评价规则、不调参、不重跑远端训练。

## 已核实的运行事实

MLP-PPO运行 `20260920T203641_73d4515809fe` 完成100回合、14281环境步、2950次更新，无记录错误。按每景 `ceil(N_demand/64)×10 epochs` 求和恰为2950，没有把回合数或平均统计伪装成更新次数。

训练末25回合采样平均U=0.7217601738、平均SKIP比例=0.0067600473；末回合U=0.7014419906，135束只跳过1束。相反，冻结确定性validation的15景中12景全SKIP，合计2021个正需求只分配16束，全部零硬约束违约。两者分别是训练采样轨迹与独立确定性评价，不能混为同一指标。

## 实际最终权重的单状态证据

使用已核验检查点 `960331c1325a4d8b83fedf46fbc3a610`，SHA256为 `1be658b221d2f511c91cf65373601c78723a644f1859000311520e1ef1bccaba`。通过 `artifacts.load_checkpoint` 与 `restore_rng=False` 在本地CPU加载，未更新任何权重。

输入为原有validation场景 `cover_output_0` 的第0步，145个正需求、当前稳定beam_id=45；共有9550个合法ALLOC及1个SKIP。实测：

- SKIP概率：**0.0054818000644**。
- 全部ALLOC概率总和：**0.994518078918**。
- 最大单个ALLOC概率：**0.00221570022404**，动作是起点89、长度10、功率索引1。
- SKIP概率为最大单ALLOC的 **2.474071倍**；全联合argmax返回ID=0，即SKIP。

Actor计算的是 `P(ALLOC动作)=P(ALLOC)×P(具体分配|ALLOC)`。确定性接口比较完整动作的概率，不比较“所有ALLOC概率之和”和SKIP。这一首状态虽然约99.45%的采样概率落在ALLOC上，联合概率最大的单个动作仍是SKIP。该现象直接解释了本状态采样与确定性解码的分离；没有逐步提取全部验证轨迹概率，不能据首状态断言全部低分原因均已证实。

## 一致性与检查范围

PPO、控制器、注册配置、评价、环境、动作、物理、数据加载、Config及共享网络共10个本地源码SHA与训练run清单完全一致；源CSV hash也与冻结数据清单一致。训练/评价均沿相同联合分布和合法mask，控制器采集时保存旧logp/value，采集期间不更新网络；GAE、clip、终止/截断及mini统计未发现此次低分对应的可复现错误。

必要复核执行了现有PPO测试：**21 passed、1 CUDA skipped，7.64秒**；另有独立概率反例验证上述机制。实际权重诊断仅做一个已有validation首状态的前向与argmax，未使用test、未重新训练、未改变冻结参数或远端任务。本地PyTorch为2.3.0+cpu，原训练为2.6.0+cu124；本次不是跨版本位级重现声明。

原始计数、概率、首状态向量、最高概率ALLOC与源码指纹见[原始数值JSON](D:/graduate/第二阶段T-SAC实施记录/PPO确定性解码原始数值.json)。当前证据支持“冻结确定性解码下表现退化，存在已验证的概率分散机制”，不支持“零违约即算法有效”，也不支持“低U即代码故障”。若未来研究其他解码方式，应另立具名消融；不能用其替换本轮已经冻结的主结果。
