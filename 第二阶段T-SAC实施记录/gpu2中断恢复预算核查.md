# gpu2 中断恢复预算核查

2026-09-21。依据本地[环境与中断计时盘点](D:/graduate/第二阶段T-SAC实施记录/gpu2环境与中断计时盘点.json)及[恢复前结果核查](D:/graduate/第二阶段T-SAC实施记录/gpu2恢复前结果核查.md)，仅生成本文和[机器可读预算核查](D:/graduate/第二阶段T-SAC实施记录/gpu2中断恢复预算核查.json)，未执行 SSH、改源码/计划或启动训练。

**仅恢复 Residual seed43，从持久 150 回合检查点继续，最多新增 150 回合，恢复阶段训练计时预算为 5940 秒（99 分钟）。** 两个 CNN 已完成 300 回合；Residual42 用时 10836.05 秒、在墙钟上限后完整结束于 271 回合，均不再训练，不给 seed42 补 29 回合。

## 时间扣除依据

`sacct` 记录步骤 `206178.4` 于北京时间 **08:44:03** 开始、**12:35:45** 以 `CANCELLED` 结束，时区 `+0800`，整个步骤为 13902 秒。这个步骤包含多组先后/并行任务，**不能把整个 campaign 的 13902 秒扣给 seed43 单组**。

seed43 的 run_id `20260921T031515_931ec8ab244a` 含 UTC **03:15:15**，即北京时间 **11:15:15**。`create_run` 在初始化后、训练计时前执行，且 run_id 时间按秒截断，因此将该时刻视为训练起点偏保守：

- 已用估计：12:35:45 − 11:15:15 = **4830 秒**。
- 再加保守裕量 **30 秒**，已计 **4860 秒**。
- 原单组预算 10800 − 4860 = **5940 秒**。

这是**训练耗时的保守估计上界，不是 checkpoint 中保存的精确累计秒数**。它包括持久 150 回合之后已丢失的至少 10 个完整回合和活动尾部；不能只按检查点 150 回合所耗时间扣除。中断之后至实际恢复前的停机时间不计为训练。恢复沿用完整回合边界收尾，实际耗时可能略超计时阈值，不因此增加回合预算。

恢复来源为 checkpoint `180cc4eb1a4d44768287ec6201bedfeb`，计数 **150 回合 / 21294 环境步 / 20295 更新**。盘点内 SHA256 为 `37a368f168fc7c06f234ec987069a8106e29f46abf55b7ee0c42ea891d108bf5`；活动快照中的 160 回合不是新的持久恢复边界。

## 保存频率与资源分配风险

恢复阶段将 `checkpoint_every` 从 **50 改为 10**。这是现有严格 resume 规则明确允许的运行期保存频率变化，用于减少再次中断后未持久工作的损失；仍保存 50 的倍数检查点，便于共同预算比较。模型、数据、奖励和优化设置不变。本文记录该操作约定，未自行修改计划或配置。

gpu2 当前仍使用 **interactive allocation `206357`（BatchFlag=0）**。把训练放入 detached 子进程不等于获得独立于 SLURM 分配的生命周期：原交互资源分配关闭时，子进程仍可能被终止。应持续保存持久检查点，**不能承诺断网或关闭交互分配后训练绝对不受影响**。
