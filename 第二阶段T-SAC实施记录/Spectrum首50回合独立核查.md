# Spectrum首50回合独立核查

核查对象：[09:29跟进快照](D:/graduate/第二阶段T-SAC实施记录/Spectrum跟进快照20260921-0929.json)。其嵌入更新时间为2026-09-21 01:30:19.669554 UTC，即北京时间09:30:19。本次只读核对结果，没有修改源码、参数或远程任务。

**seed42两组首50回合可配对，但当前不足以宣布修复有效；两组验证均值都低于源CNN父模型。seed43尚无残差结果，不能配对。**

## seed42配对证据

CNN继续训练与Spectrum残差的检查点计数均为**新增50回合、7128环境步、6129次更新**，请求记录与评价报告内实际计数一致。两份完整validation各含15个唯一场景；场景ID、实体/需求数、总需求、6000W预算及phase逐项相同。30条评价记录的违约计数与列表均为零，功率未超预算。

- CNN检查点：`2a816914f348424dac5bab5d94d03efe`，平均U **0.8766088470**。
- 残差检查点：`485e328a754e49bfb89e0e2748f2519f`，平均U **0.8819079782**。
- 残差−CNN：**+0.0052991311**，即约+0.530个百分点；15景中10景较高、5景较低。
- 按场景ID配对、随机种子0、2000次bootstrap独立重算95%区间为 **[-0.0072603534, 0.0159287591]**，与快照一致；区间跨零。

均值与原报告的差异小于1×10⁻¹⁵，仅有浮点求和顺序差异。这是15场景的条件性区间，不是训练seed区间。快照中的实时训练已超过50回合，但本页只比较上述冻结检查点，不把实时进度混作评价预算。

## 必须保留的父模型参照

源CNN的100回合validation平均U为**0.8903744222**。已对照[源验证逐场景原件](D:/graduate/第二阶段T-SAC实施记录/最终结果原件20260921-0601/论文七基线完整比较/验证集逐场景结果.json)，它与本次seed42两组的15景场景/需求/资源签名一致。

- CNN继续训练50回合相对父模型：**−0.0137655752**，约−1.377个百分点。
- 残差新增50回合相对父模型：**−0.0084664441**，约−0.847个百分点。

不能只报告残差相对继续训练对照的正差而隐藏双方低于父模型的事实。源模型已有100回合训练经历，新任务重置优化器、Replay、RNG与计数；新增50回合不等于从零只训练50回合。两个续训随机流共享同一个预训练父模型，也不是从零独立的多seed证据。

## 尚未具备的结论

seed43只有CNN首50回合validation结果：U=**0.8578150611**，预算为50回合/7006环境步/6007更新；残差模型在该快照仍为queued，配对状态正确标为pending。不能用seed42残差与seed43 CNN替代同seed对照，也不能把不同seed的环境步数差异当成协议失败。

整批仍未结束，最终评价尚未产生，`test_set_used=false`；本核查只读取训练进度和validation原件，未读取test结果。单次中期点估计、跨零区间及父模型参照应一起保留；没有依据本次分数改参、重跑或改变冻结解码。检查点文件SHA和持久化由本轮主交付核查另行记录，本页不把快照审阅冒称为再次下载并核验权重。
