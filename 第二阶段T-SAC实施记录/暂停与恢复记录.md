# 第二阶段 T-SAC 暂停与恢复记录

> **2026-09-21用户临时断网暂停：主动工作与自动跟进已暂停，等待用户明确说“继续”。已提交的gpu4任务未中断，将按原预算运行归档。最新恢复入口：[Spectrum临时断网暂停与恢复记录](D:/graduate/第二阶段T-SAC实施记录/Spectrum临时断网暂停与恢复记录.md)。下方为此前阶段记录。**

> **最新恢复指向（2026-09-21 06:01）：恢复授权后的两批训练、评价及远端持久化已完成，Suite completed；10方法各15validation和15test完整零违约。本次授权实验工程流程完成，详见《论文七基线最终对比与夜间训练验收报告.md》及两份远程状态JSON。结果同步完成后暂停自动跟进，不重复启动。严格广覆盖/root未知、日志5%目标及单seed/PPO解码解释限制保留，不宣称整项研究完美通过。下方旧恢复/暂停描述均为历史。**

> **历史恢复指向（2026-09-21 04:39）：用户已恢复并授权gpu4论文七基线；当时父实验完成、五法GPU短验通过，基线训练仍进行。详见对应阶段记录；该状态现由上方06:01更新。下文保留9月20日暂停历史，“未收到继续前不执行”不代表当前授权。**

状态：**用户于2026-09-20明确要求暂停并关机；未经用户说“继续”，不执行上传、训练或后续实施。**

## 用户要求与固定边界

- 工作目录 `D:/graduate`；依据两份第二阶段设计v3文档实施。此前“只更新方案”限制已由用户明确解除。
- 新算法名 **Horizon T-SAC**，目录 `PPO4090/implementations/horizon_tsac_20260920/`；保留旧 `sac_fyh_io`、原始CSV和历史产物。
- 范围包括算法/环境、日志、五模块前端、广覆盖数据接口。最终必须同时交付**验收报告与code review**。
- 本地编辑、先本地测试，再上传远程做短验证；正式长训练另行安排，不自动启动。
- 所有用户交付说明使用中文文件名；技术模块保留稳定标识。

## 已保存实现

- 集中Config/project_paths、guarded train/evaluate/export、run身份/原子工件/检查点及恢复。
- data schema/loader/augment/split/domain：稳定beam_id、实体与业务mask、显式旧坐标映射、固定group、独立顺序RNG、root/candidate守恒和球面归属、谱系划分、域审计/采样。
- env：9551候选联合动作、严格合法性、SKIP、完整终局账本、每束总功率槽内均分、统一float64信道/干扰、100ΔU、独立评价及显式第三阶段continuous模式。
- models/rl：四种编码器、独立/条件Actor、可加/联合Critic、精确期望SAC、微批与候选分块、完整回合回放、ActionSpec签名隔离、恢复引用。
- telemetry/dashboard/frontend：独立writer、SQLite WAL FULL、事件去重和步事务、delta/关键帧/状态hash、优化窗口、诊断镜像及异常CPU缓冲、只读REST/SSE/integrity、五模块React/TypeScript离线构建。
- research_stats：真实phase、兼容性、恢复去重、缺seed、场景/seed层级bootstrap、正式ranking保护；四个同完整观察2×2配置。
- 新增可配置更新协议：旧配置缺字段默认episode；smoke明确episode；7份研究配置明确env_step、4000回合/2000步热身。这里只写配置，**没有运行4000回合**。
- README路由和AGENTS已更新；原始126个冻结文件未改动。

## 已执行证据

- 最近一次全套：**104 passed，18 warnings，72.13秒**。其后还有少量日志、评估横轴、统计和调度收尾改动，恢复后需跑最终全套并更新总数。
- 更新调度8项实现测试＋4项独立审查测试，共12项通过；env_step完整恢复逐位一致，warmup/截断/旧checkpoint兼容通过。
- 日志/交付专项最近32项通过；TypeScript＋Vite正式构建通过；真实浏览器五模块、联动、回退/reset、SSE重连/历史不抢占、独立离线包已验收。
- 3束4槽fixture穷举3375序列，U*=1；180更新后U从0到1。仅逻辑学习验证，不是研究结论。
- 主模型220正需求/100槽/9551动作，full_attention d128/4头/2层，CPU batch2/micro1更新1.628秒；进程累计峰值约291.57MiB。未验收GPU batch256。
- 真实CSV两回合139+147束，286步、2更新。运行目录依次为：
  - `20260920T112941_a0b1f0bca95e`：早期过程记录，旧工件引用问题修复前，不作最终包。
  - `20260920T114236_e4949de2fada`：修复后真实短跑，Web关闭期间照常跑完；下面有2景×4策略独立评估。
  - `20260920T114918_d9ae5e0a98b7`：关闭日志对照。
  - `20260920T115339_84e548299997`：稀疏日志优化后对照；之后有少量日志/引用/调度收尾改动。
- 两景validation平均U：短训确定性policy=0（全SKIP、尚未收敛），random=.73207448087878，受限16候选greedy=.9353268011548699，25W等功率=.9096864742677213；违约均0，导出复算通过。
- 日志开关模型/α逐位一致，但单对CPU短跑4.062秒→16.938秒，**5%日志开销目标未通过**。十万记录查询优化后分页P95 24.26ms、全文24.60ms、指标视口267.33ms。
- 严格独立root广覆盖验收不通过：上游生成器/root/assignment缺失，100CSV未知谱系，52–168正需求，必需单元root配额覆盖0%。不使用fixture冒充新训练数据。

## 远程状态（重要）

用户已完成本机密钥生成、服务器公钥授权、Windows ssh-agent启用和解锁。本机私钥实际路径：`C:/Users/minyu/.ssh/HorizonT-SAC`；不读取或输出私钥内容。连接配置：本目录 `远程连接配置模板.json`。

已成功验证两跳：

```text
本机 → u2024110224@10.160.16.3:22854 → workstation → ssh gpu1 → gpu1
```

**不能在workstation登录节点训练。** 用户明确要求ssh gpu1后才能正常跑。

只读探测结果：

- GPU1用户home：`/home/u2024110224`。
- GPU：**NVIDIA A800-SXM4-80GB**，当时0MiB占用/0%利用率；不是4090。
- 用户已有SLURM作业：`205952,g078t,interactive,RUNNING,gpu1`。关机后/恢复时需重新查询，不能假定作业仍在。
- 分区g078t有8节点、gpu:a800:8；g125t当时无节点。
- 默认 `/opt/app/anaconda3/bin/python3` 为Python3.9.12，不适合直接运行本实现。
- **用户DRL环境**：`/home/u2024110224/.conda/envs/DRL/bin/python` 为Python3.10.16，优先继续检查它的torch/CUDA和依赖；尚未读取这些版本。
- 系统 `/opt/app/anaconda3/envs/pytorch_2.3.0/bin/python` 实测Python3.12.9，不能仅凭环境名推断版本。
- 用户填写的远程目录：`/home/u2024110224/PPO4090/implementations/graduate_horizon_20260920`。下一步应在它下面新建独立部署子目录，包内PPO4090作为运行根目录，保留现有项目。
- **尚未上传源码包、未在远程创建环境、未启动远程训练。** 远程只做了上述只读检查。

可重用连接方式（用户说继续后才执行）：

```powershell
ssh -p 22854 -i 'C:\Users\minyu\.ssh\HorizonT-SAC' -o IdentitiesOnly=yes -o BatchMode=yes u2024110224@10.160.16.3 "ssh -o BatchMode=yes gpu1 'hostname; pwd'"
```

Windows关机后ssh-agent服务是Manual，可能需重新Start-Service/ssh-add并由用户输入私钥保护口令。服务器公钥已经接受；不要再次添加重复公钥、不要询问或记录服务器密码/私钥口令。

## 恢复后的顺序

1. 读取本记录、AGENTS、项目说明、project_paths及两份v3；保留当前已实现代码，不从头重做。
2. 核对当前源码、各审查闭环和暂停时文件；运行最终pytest、AST/非训练导入、原126文件hash、前端构建（只在变化后重做必要检查）。
3. **尚需收尾的真实问题**：
   - 训练run应冻结一份当时的`训练场景覆盖审计.json`并publish为artifact，离线包不应借用当前工作区的新审计。前端已改显式run-root时默认从包内查，缺失则unavailable。
   - 复核默认train.prepare_dataset仍使用`legacy_manifest`而非D0已带domain_cells的冻结清单；默认真实训练的DomainSampler因此可能全部unknown。要把冻结域标签/采集计数接入真实训练，不能只在单元fixture验证分层采样。改变清单完整内容会改变manifest_hash，旧checkpoint需显式沿用旧数据划分，不能混用。
   - 最新evaluate已修每策略run累计evaluation_env_step、JSON文件hash与血缘hash区分、ID安全子目录、纯推理/rollout计时；需最终对应回归。
   - 研究配置4000回合已经修复热身不足，补齐报告中的最新状态；不要运行正式预算。
4. 本地最终短跑/独立评价/一致性包按必要新改动验证；保持真实0分短训结论和日志性能不达标记录。
5. 检查gpu1 DRL环境torch/CUDA/依赖及用户SLURM资源有效；打包新实现并部署独立目录，GPU1短测，不在workstation跑、不启动长训。
6. 执行`python -m implementations.horizon_tsac_20260920.package`生成中文运行包和SHA清单。目前package.py已写，**最终包尚未生成**。包不含私钥、venv、node_modules或历史模型。
7. 更新并最终交付《第二阶段重构验收报告.md》《第二阶段重构代码审查报告.md》，附实际远程结果、完整验收数字及限制。两份目前已创建但仍是收尾中的记录，不能直接当最终全通过报告。

## 文档与进程

本目录保存基线、进度、环境/数据/算法/日志/交付/调度独立code review、真实JSON性能数据与浏览器验收。前端使用说明在 `PPO4090/frontend/horizon_tsac_20260920/看板使用说明.md`。

暂停时所有算法/环境子代理已完成，仍运行的前端代理已中断。只读看板原服务PID47196（8765）将关闭；8766临时验收服务此前已关闭。没有本任务启动的远程训练。

临时验收目录 `PPO4090/.tmp-dashboard-package` 保留：子代理尝试递归删除时被自动审批拒绝，仅返回`blocked by policy`，没有进一步原因；未尝试绕过。它不是正式交付包，不影响源码或历史数据。
