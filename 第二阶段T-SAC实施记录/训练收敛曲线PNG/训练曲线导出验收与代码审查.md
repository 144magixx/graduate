# 训练曲线导出验收与代码审查

本次生成15张240 dpi PNG：三η Horizon、五学习基线、四个同源续训流各一张，以及三张分组对比。共1915条有效逐回合记录。源文件SHA复核不变，PNG解码/尺寸/哈希检查全部通过；图面主标题、参数栏及页脚经过渲染边界检查，并实际查看Horizon、基线总图、PPO、局部T-SAC和Spectrum恢复图，无遮挡或缺字。

独立核查确认：历史8组的全部800个绘图点、配置和run_id与原件一致，三个η映射正确；CNN/MLP/局部T-SAC的实际SAC学习率一致，DQN与PPO专属参数正确。PPO显示状态价值V、GAE、clip和on-policy参数，不把公共配置中的Replay、α、τ当作生效参数。审查发现的局部T-SAC内部编码名、两个CNN续训对照的JSON算法ID均已修正并重新导出。

Spectrum seed43只拼接旧1–150和恢复后的151–244回合；旧尝试151–160另存原件，未重复统计。所有主曲线回合编号连续、环境步严格递增；U有限且在[0,1]，每回合实测回报满足R=100U。移动均值为后向10回合，开头min_periods=1，恢复点重新起算；无未来数据泄露，无跨seed平均或伪造置信区间。

当前seed43 PNG是北京时间2026-09-21 14:18快照，不能作为最终训练图。Fixed/Greedy/Random没有学习过程，不生成假曲线。图中只使用训练U/回报，不把其当作独立validation或稳定收敛证明；没有修改模型、超参数、训练进程或使用test选参。后续完整训练图应另存新版本并保留当前快照。
