# gpu4 夜间并行训练交付记录

北京时间2026-09-21 01:53启动。用户已明确恢复任务，并授权在gpu4并行长训练；不再使用gpu1。

## 已完成的部署前检查

- 本地最终全套：146 passed、1 skipped（本地无CUDA）、18 warnings，48.15秒。前端TypeScript/Vite构建通过。
- 全项目AST 88个文件、非训练导入48模块通过；126个旧实现/原数据/历史产物哈希无变化。
- gpu4核心测试133 passed，新增监督器/设备恢复/SAC补验36 passed（两组有重叠，不相加作测试总数）。
- GPU真实CSV短验证：286环境步、148次CUDA更新，两个回合U分别0.6081596401700562和0.7054066246650955，均满足增量回报与硬约束。CUDA检查点可在CPU独立评价，短训确定性验证U仍为0，未掩盖冷启动结果。
- A800容量短测：batch64/micro64，稳定更新0.212–0.230秒，峰值已分配显存967568896B。micro16相同有效batch约0.748秒；仅调整计算分块，没有改变候选概率归一化或SAC公式。
- 修复长训练运行问题：实际执行checkpoint_every、结束时保存完整边界、有限墙钟预算；GPU初始化前设置CUBLAS确定性环境；CPU评价跳过CUDA动作RNG恢复；不同超参数不再误合并为同一seed组。
- 默认数据读取冻结域清单并消费真实domain_cells；run内保存当时的覆盖规格和审计。训练70景范围124–165，validation15景52–157，test15景126–168，未知root限制保留。

## 三组受控实验

仅改变目标熵比例η：0.2、0.5、0.8。共同seed42，完整注意力d128/4头/2层、条件Actor/联合Critic、100槽与9551候选、预算6000W、100ΔU奖励与γ=1保持一致。

共同优化协议：actor_lr=1e-5、critic_lr=1e-4、alpha_lr=1e-4、初始α=.01、τ=.005；有效batch64、microbatch64、Replay容量4000、热身1000步，此后每环境步1次更新，仅从完整回合池采样。相较名义研究配置的batch256/Replay20000/热身2000，今晚使用明确具名的资源与时间预算，三组保持一致；不把它们当作已运行4000回合。

每组目标100个完整回合，训练墙钟上限16200秒（4.5小时，在回合边界停止），每25回合及最终完整回合保存检查点。监督器并发3个训练，CPU评价串行；每进程torch/BLAS线程数1。共享已分配的一张A800，不申请或使用其他用户的GPU。

首个检查点自动做3景快速validation；训练结束做全部15景validation，并给合法随机、至多16候选贪心与25W等功率基线。若各组最后完成预算不同，另选三组共有且环境步/更新步一致的最高保存回合，补做全validation的policy对比。测试集不参与选参；单seed结果不构成正式五seed结论。

## 实际进程与保存位置

- 登录节点：`u2024110224@10.160.16.3:22854`，计算节点：`gpu4`。
- 已有SLURM分配：`206178`；后台步骤：`206178.1`；workstation上的srun控制进程PID：`1455573`。
- 该分配有1张A80080GB、8CPU、120GB RAM，查询时有效至2026-09-24 01:17:53。
- 节点本地项目：`/tmp/HorizonT-SAC-20260921-tWxdou/PPO4090`。活跃SQLite/WAL只在此本地磁盘运行。
- 持久项目：`/home/u2024110224/PPO4090/implementations/graduate_horizon_20260920/HorizonT-SAC-20260921-v1/PPO4090`。
- 计划/进度/最终比较：持久项目下 `夜间实验/HorizonT-SAC-20260921夜间超参数对比/`。
- 完整run及独立评估：持久项目下 `outputs/horizon_tsac_20260920/`。运行中同步已发布不可变工件，首检查点在线backup，结束后闭合数据库备份并完整复制；不直接复制活跃WAL。
- 启动日志：部署目录 `supervisor.log`；每组训练/评价日志在上述夜间实验目录，均为中文文件名。

训练run_id：

- η=.2：`20260920T175318_d20568cffeaa`
- η=.5：`20260920T175318_b8b9d93a0d79`
- η=.8：`20260920T175318_d20baa2bfb80`

启动后已确认三组各越过1000步热身并执行180余次真实更新，状态running，已提交错误数0，nvidia-smi看到3个训练进程，各约1.6GB显存。这里是启动验收，不是最终训练成绩。

## 跟进与真实限制

远端监督程序独立于本机SSH会话，生成`夜间进度.json/md`和`夜间最终比较.json/md`；本机关闭不影响已分离的远端进程，但取消SLURM分配或节点故障会终止计算。另已设置本任务每30分钟跟进，正常未变时静默，完成/故障/有可比较结果时报告。

不可保证预设性能提升；训练失败、预算未齐、验证缺失和副本失败都明确记录。节点临时盘当时约21GB可用，100回合/每25回合归档将每组完整Replay快照限制在4份左右，不采用每回合保存4000次的危险默认行为。

广覆盖生产数据、物理来源和正式多seed结论仍未完成。原CPU轻量短跑的日志5%开销目标未通过；今晚保留全部基本轨迹，在真实GPU训练协议下另看实际成本，不通过关日志伪装达标。

源码包SHA256：`ade9a49ddd7ed490bdc5f8a927a1e7098f09c146df68191552afb1fb0712f0e1`。源代码已固定后启动，今夜不会悄悄更改超参数或重写已有实验结果。
