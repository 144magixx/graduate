# 开题报告后续研究 · 问题清单（Q&A 索引）

> 用途：用户按编号逐条提问，助手逐条作答。
> 依据材料：
> - `开题报告-导出1212最终版.docx`（题目：HTS智能波束覆盖及频率-功率资源分配算法研究）
> - `Transformer-Enhanced SAC for Joint Power–Frequency Allocation in HTS Systems.pdf`（第一阶段成果论文）
> - `PPO4090/`（代码：`Environment_sac_p.py` / `Environment_fyh_IO.py` / `sac_transformer_p.py` / `sac_transformer_tok.py` / `sac_fyh_IO.py` / `sac_train_*.py` / `greedy.py` / `cover_output_list/`）

---

## A. 第一阶段现状与不足（诊断）

| 编号 | 问题 | 关联证据 |
|---|---|---|
| A1 | 开题报告把三阶段写成"逐层递进"，但代码里第二阶段是**顺序贪心 + 每波束单步决策**，第三阶段完全没有代码。这个"递进"关系在工程上到底怎么落地？ | `sac_train_p.py:155` while not done，每步只处理 `env.beam_idx` |
| A2 | 奖励函数主体是 `sat + sgm_rate - occupy_penalty`，而 `sat` 在 `satisfaction_increment >= 1` 时取 +1、否则取 −1，近似**二值常数**。这还能提供有效的策略梯度信号吗？ | `Environment_fyh_IO.py:1056-1061, 1072-1092` |
| A3 | `interfere_penalty` 被注释掉了（`# - interfere_penalty`），奖励里**没有任何干扰项**。那论文里"平均 SINR 提升 2–3 dB"是靠什么机制学出来的？ | `Environment_sac_p.py:944`、`Environment_fyh_IO.py:1081` |
| A4 | 训练脚本每步都做一次梯度更新（100 步/回合 × 4000 回合），但奖励是**单波束即时奖励**，没有 bootstrap 到"整回合分配质量"。折扣因子 γ=0.99 在 100 步视界下是否合适？ | `sac_train_fyh_IO.py:252-261`，γ=0.99 |
| A5 | 三个独立 Categorical 头（freq/slots/power）+ `_joint_q` 把 Q 值**相加**，等于假设三个动作维度可加可分。这个分解假设对联合分配问题成立吗？ | `sac_transformer_p.py:335-349`、`417-436` |
| A6 | `target_entropy = -6.9` 与动作空间规模的关系是怎么定的？三个头熵求和（最大约 ln(100)+ln(10)+ln(10)≈9.2）与 −6.9 的匹配依据？ | `sac_train_fyh_IO.py:101` |
| A7 | `freq_dim` 在 `sac_train_p.py` 里是 10、在 `sac_train_fyh_IO.py` 里是 100；Freq 动作到底代表什么？"起始槽索引"还是"频组内块索引"？ | `sac_train_p.py:114` vs `sac_train_fyh_IO.py:133` |
| A8 | `sac_train_fyh_IO.py` 直接把 `action[0]`（0–99）当起始槽；`sac_train_p.py` 却走 `env.get_first_free_slot()`。两条路径的语义不一致，哪个是论文结果？ | `sac_train_p.py:163` vs `sac_train_fyh_IO.py:236` |
| A9 | 论文约束 (7c) "频率连续"在环境里**没有实现**（越界直接截断 `action[2] -= out`），`get_first_free_slot` 已被绕过。这算不算"论文与实现不符"，重构时怎么补？ | `Environment_fyh_IO.py:962-966` |
| A10 | 论文 (3) 式说"同极化 **且不同频组**"才产生干扰；代码 `interfere_beams` 只排除同极化，**没有排除不同频组**，等于把同极化的所有频组都当干扰源。这是符号错误还是有意简化？ | `Environment_fyh_IO.py:643`（对比论文 II-B 式(3) 的 x^s_{i,j} 定义） |
| A11 | 频组分配写死为 `beam_idx % 8`（轮询），论文动作空间 (9) 却说动作含 `γ_b(t)`（起始 FS）且 `y` 由环境静态指定——那论文关于"频组也是待优化量"的表述是否要改？ | `sac_train_*.py:162/235`、`greedy.py:57` |
| A12 | 总功率约束 `Ptot=3000W` 是**硬终止**（超了就 `done=True` 提前结束回合），不是惩罚。回合提前结束会让 `total_satisfaction` 只在已分配波束上求和，指标被系统性高估。怎么修？ | `Environment_fyh_IO.py:991-995`、`calculate_total_satisfaction` 用 `[0:beam_idx+1]` |
| A13 | 每波束功率上限 P_max（论文 (7e)）在环境里**完全不存在**，只有 `Ptot`。`P_MAX=50` 只用于 `power_gap` 归一化。 | grep 全仓：无 Pmax 约束实现 |
| A14 | 波束数是模块级常量 `beam_num = 100`，状态维度 100/101/202/205 硬编码；论文声称"共享参数可适配不同波束数"，代码上做不到。重构时怎么参数化？ | `Environment_*.py:18`、`sac_transformer_p.py:52-62` |
| A15 | 干扰/速率模型里 `phi` 用**地面两点球面距离**近似离轴角，而论文式(2) 的 `θ_{b,i}` 是星上离轴角。这个近似在天顶/边缘波束差多少？ | `Environment_fyh_IO.py:660-663, 802-805` |
| A16 | `self.history_beam[conflict_beam]` 按"列表下标 = 波束 ID"取值，依赖 `history_beam` 严格按波束序追加。这个隐式耦合重构时怎么消除？ | `Environment_sac_p.py:590` |
| A17 | `BeamInfo.__init__` 里 `np.random.seed(None)` 破坏可复现性；`rate` 用 `np.random.choice(valid_rates) + N(0,20)` 生成，与"真实业务分布"差距多大？ | `Environment_sac_p.py:255-263` |
| A18 | 论文仿真里"100 个中国主要城市 + 需求随机生成 + 按需求降序索引"，`BeamInfo` 里 `argsort(-rate)` 把波束**按需求排序**。这等于给智能体泄露了全局排序信息（先服务的都是大需求波束）。这算不算信息泄漏？ | `Environment_sac_p.py:273-278` |
| A19 | 代码里 `state = env.reset(...)` 之后又有 `state = env.reset(beam_info)` 调用两次，第一次返回值被丢弃；`CurrentBeamState.update_state()` 在 reset 里被调用一次、每次 step 又调一次，beam_id 计数是否对齐？ | `sac_train_p.py:151`、`Environment_sac_p.py:412-413` |
| A20 | `Environment_sac_p.py:371` 的 `cinr_loss_cache` 标注 `TODO`，`avg_cinr_loss_db` 计算后又在奖励里被注释掉。第一阶段的"干扰抑制"结论是否有可复现的定量支撑？ | `Environment_sac_p.py:932-938` |

## B. 第一阶段论文（T-SAC）的评审风险

| 编号 | 问题 | 关联证据 |
|---|---|---|
| B1 | 论文正文写 "improves the average **CINR** by about 2–3 dB"（摘要和结论都写 SINR）。指标名混用怎么统一？ | 论文 V 节、Fig.5 讨论段 |
| B2 | 基线 7 个，但 Greedy 用固定 3 档功率 `{20,25,30}W`、Random 是下界，DQN/PPO"接近随机水平"。**没有把 SCA/WMMSE 凸优化基线**放进对比（而开题报告第 3 阶段要用 SCA）。审稿人会问"为什么不和数学优化比" | 论文 IV 节 baseline 列表 |
| B3 | Fig.5/6/7 只有 mean+variance，**没有置信区间、没有显著性检验、没有随机种子数说明**。10 个随机场景的方差条能否支撑"smallest variance in all scenarios"？ | 论文 IV 节 |
| B4 | "reduces the variance by more than 70%" 是相对哪个基线、哪个指标的方差？没有给统计量定义。 | 论文 IV 节 |
| B5 | 训练收敛曲线 Fig.4 只有一张图，没有样本效率指标（达到某性能所需交互步数）、没有 wall-clock、没有显存/推理延迟。而论文主打"sample efficiency"。 | 论文 IV 节 |
| B6 | 论文声称"Shared encoder parameters allow the model to adapt to different beam numbers without structural changes"，但**没有做 beam 数变化实验**（只有 Nb=100）。这条泛化性主张如何坐实？ | 论文 III-B |
| B7 | 论文声称 Transformer 捕获"long-range cross-band interference patterns"，但消融只有 MLP-SAC / CNN-SAC，**没有去掉位置编码、没有去掉 Transformer 层数、没有 head 数消融**。 | 论文 III-B / IV |
| B8 | 论文 (7a) 约束"每个 FS 最多被一个波束占用"在环境里被放松为"同频组内可叠 8 个"（`mask = counts < 8`）。为什么是 8？ | `Environment_sac_p.py:800-804` |
| B9 | 论文说"SAC agent uses maximum-entropy to learn optimal policy over the **discrete** action space"，但 (16) 式写的是 `α log π_θ(a|s) − min Q`，这是连续 SAC 的写法；离散 SAC 应为 `π^T(α log π − Q)`。公式与实现对不上。 | 论文式(15)-(19) |
| B10 | (18) 式 TD target 写成 `r + γ(min Q̄ − α log π)`，而离散 SAC 正确形式是 `r + γ(min Q̄ + α·H)` 或 `r + γ·E_π[min Q̄ − α log π]`；代码 `calc_target` 用的是 `exp_min_q + α·entropy`（**加**号）。公式、代码、标准算法三者不一致。 | 论文式(18)、`sac_fyh_IO.py:346` |
| B11 | 论文 Table II 里 replay buffer = 20000 steps，而代码 `capacity=20000`；但每个 episode 有 100 个 transition，等于回放池只装得下 200 个回合的经验（4000 回合里最新 5%）。这算 on-policy 化了吗？ | 论文 Table II、`sac_train_fyh_IO.py:94` |
| B12 | 论文 Table I 里 "Number of Power levels = 10"，功率档实际是 `10*log10((idx+1)*5)` → 5..50W 十档。但论文没给 `Pmax`、`Bmax`、`Ptot` 三者与 level 数的关系，也没有说明 `ω` 的选取依据。 | 论文 Table I、`sac_train_*.py:238` |
| B13 | "The RL agent is first trained at the ground gateway until convergence and then deployed on the satellite" —— 星上推理延迟/算力预算有没有量？Transformer 128 维 × 2 层 × 101 token 在星载 DSP 上可行吗？ | 论文 III 节开头 |
| B14 | 参考文献 [4] 在正文被引为 "NP-hard problem"，但 [4] 是 Aravanis 的 two-stage power allocation（正是你第 3 阶段要用的方法）。引用是否贴切？ | 论文 I 节、[4] |
| B15 | 论文 IV 节写 "seven typical baseline algorithms"，列出 7 条（含 Fixed/Greedy/Random/MLP-SAC/CNN-SAC/MLP-DQN/MLP-PPO），但讨论里反复把 Greedy 归入"learning algorithms can learn..."的对比。表述是否清楚？ | 论文 IV 节末 |

## C. 第二阶段（T-SAC 频率-功率联合分配）如何开展

| 编号 | 问题 | 关联证据 |
|---|---|---|
| C1 | 第二阶段要"基于智能体给出的波束覆盖方案"，但**覆盖方案目前是 `cover_output_list/` 里 56 份预生成 CSV**，不是第一阶段智能体输出。这个接口怎么定义（覆盖方案 → SC-MDP 的耦合变量有哪些）？ | `cover_output_list/`（201 行/份，列 `lat,lon,rate,beamwidth`） |
| C2 | `cover_output_list/*.csv` 那 56 份布局是怎么生成的（聚类？k-means+最小覆盖圆？）。它是"候选动作空间"还是"已定方案"？ | 同上 |
| C3 | 第二阶段是"顺序决策"（按波束顺序逐个分配）还是"联合决策"（一次输出全部波束）？顺序决策会引入**顺序偏差**（先决策的波束占优）。怎么证明无偏？ | `Environment_fyh_IO.py` SC-MDP 逐波束 step |
| C4 | 顺序决策下，先分配的波束看到的干扰少、后分配的被前面挤占。是否要加**随机波束顺序**训练 + 推理时多序投票？ | 同上 |
| C5 | `sac_transformer_tok.py` 把每个频组内的 (干扰, 占用) 展成 10×10 → `slot_embed(20)`，token 数变成 12；`sac_transformer_p.py` 是 101 个 token。哪个版本是最终方案？两者结果可复现吗？ | 两文件对比 |
| C6 | `sac_fyh_IO.py` 的 `PolicyNet.forward` **去掉了 freq_mask**（对比 `sac_transformer_p.py` 保留了 mask）。第二阶段重构时掩码要不要保留？保留会不会让 log(0) 数值不稳？ | `sac_fyh_IO.py:80-118` vs `sac_transformer_p.py:78-113` |
| C7 | 状态里 `row_norm` 是**归一化到 [0,1] 的干扰功率 dB 值**（`P_MIN_DB=-300, P_MAX_DB=-100`）——这实际等价于对信号做 log 压缩后再线性映射。这个映射合理吗？会不会丢掉弱干扰（−100 dBW 以上全截断）？ | `Environment_fyh_IO.py:10-11, 482` |
| C8 | 状态 `Ob(t)` 用的是 `freq_pool_real`（计数矩阵，0/1/2…），而不是论文式(8)里说的 `Y_b^T · X_b`。实现与论文的 SC-MDP 状态定义不符，以哪个为准？ | `Environment_fyh_IO.py:473-485` |
| C9 | 论文式(8)状态含 `Φ_b(t)`（波束位置）和 `ξ_b(t)`（剩余功率）。代码里 `ξ_b` 是 `(Ptot - total_power)/Ptot` 一维标量，**但没有作为约束喂给 actor**，只是输入特征。要不要把可行性做成动作掩码（structured action masking）？ | 同上 |
| C10 | 第二阶段宣称要保证"容量匹配率高、公平性好"。`SGM_rate` 在实现里对 `|Δ|>5e7` 加了 `−(|Δ|−5e7)/1e9` 的**人工惩罚**，这不是论文式(5)(6)的原定义。为什么要加？加了以后 SGM 还是论文那个 SGM 吗？ | `Environment_fyh_IO.py:862-866` |
| C11 | 论文式(6) λ_b 的实部虚部量纲不一致（实部是比值、虚部是 `Δ/β`），导致 SGM 对 ±Δ 不对称（过度分配惩罚轻）。实现里 SGM 对 0.8 倍和 1.2 倍需求都给出 ≈0.35，**并没有实现"对欠分配重罚"**。这是 bug 还是定义如此？ | 数值验证：χ=0.8→0.349，χ=1.2→0.350 |
| C12 | `current_satisfaction` 被 clip 到 1 后用于计算 `satisfaction_increment`，但 `total_satisfaction` 用 `np.minimum(alloc/req, 1)`。两处 clip 一致，但 `sat` 变量（+1/−1）与之无关。奖励设计要不要整体重写？ |
| C13 | 第二阶段如何证明 Transformer 的必要性？建议的消融矩阵：MLP / CNN / Transformer、有无位置编码、1/2/4 层、4/8/16 head、有无 attention mask。要不要把"参数量对齐"作为公平比较前提？ |
| C14 | 泛化性实验怎么设计才对得起"Adapt to different beam counts"？建议：训练 Nb=100 → 测试 Nb∈{50, 100, 150, 200}（零样本）；以及训练需求分布 A → 测试分布 B（跨城市/跨时段）。 |
| C15 | 第二阶段的**样本效率**怎么量化？建议指标：达到 baseline 95% 性能所需的 environment steps、收敛回合数、训练 wall-clock、GPU 小时。 |
| C16 | 第二阶段可能需要复现开题报告承诺的"容量匹配率"指标（χ_b = tp/td）。论文里 χ_b 与 SGM 同时进奖励 (7)（ε1, ε2）。ε1/ε2 取值？敏感性分析？ | 论文式(7)、`Environment` 无 ε 参数 |
| C17 | SAC 的 α（温度）自动调节：`alpha_loss = ((entropy - target_entropy).detach() * α).mean()`，标准形式是 `−(log α · (entropy − H̄)).detach()` 的方向。符号是否有误（会让 α 发散或塌缩）？ | `sac_fyh_IO.py:445` |
| C18 | 回放池用 `random.sample`（无放回）不是 `random.choices`（有放回），且存的是 numpy 拷贝。`np.array(state)` 对 100+ 维状态每步都拷贝，训练吞吐瓶颈在哪？ |
| C19 | 单次划分训练/验证/测试集了吗？`sac_train_fyh_IO.py` 每回合从 56 份 CSV 随机抽一份 + `augment_df` 增强，**没有留出独立测试集**。"泛化性"结论是测试集上的还是训练分布内的？ |
| C20 | `augment_df` 的 rate_scale ∈ [0.5,1.6]、bw_scale ∈ [0.8,1.25] 是**域随机化**。域随机化的范围怎么定？范围定得宽会不会牺牲标称性能？需要 Pareto 曲线。 |

## D. 第三阶段（链路级功率精细优化）如何开展

| 编号 | 问题 | 关联证据 |
|---|---|---|
| D1 | 开题报告写"对分配公平性指标未达标的波束执行二次功率分配"，是**条件触发**还是**全局求解**？条件触发会破坏总功率守恒（省下的功率给谁？）。 |
| D2 | 第三阶段优化变量是"每个波束一个功率"还是"每个波束每个频率槽一个功率"？论文 P1 是前者（`P^Tx_b(t)` 每波束标量，式(7e)(7f)），但 (4) 式 `tp_b = Σ_k B_slot log2(1+Γ^k_b)` 对 k 求和意味着每槽可不同 SINR。自由度到底几个？ |
| D3 | 只有频率分配固定，功率连续化后可解性问题：`max Σ log2(1+SINR)` 型问题用 SCA/WMMSE 需要**凸的干扰耦合结构**。你的 SINR 分母里干扰项是 `Σ_{i≠b} x^s_{i,b} δ^s_{i,b}`，对 `P_i` 是线性的（分子对 `P_b` 线性）。这实际是**线性分式**结构 → 可以 Charnes-Cooper 或直接 SCA。是否考虑过更简单的 Dinkelbach / 几何规划？ |
| D4 | WMMSE 的适用前提是**每个用户有独立波束成形向量**；你这里是**每波束固定天线方向图 + 功率标量**，没有波束成形自由度。WMMSE 能不能直接用？是否需要换成 SCA / 凸差规划（DC programming）？ |
| D5 | 第三阶段的"凸松弛"具体指什么？`x^s_b` 是二进制（频率槽占用），松弛成 [0,1] 后如何保证整数解？还是说第三阶段只优化 P，把 X 固定为 T-SAC 输出（这样就没有整数变量了）？如果是后者，题目里"凸松弛"这个词要不要删？ |
| D6 | 第三阶段的初始化来自 T-SAC 的**离散档位**（5..50W 十档）。SCA 从离散解出发能保证比离散解更优吗？（SCA 单调可行，可以，但要给出证明或至少数值证据） |
| D7 | 第三阶段如何处理**原始不可行**的情况：T-SAC 给的频率方案 + 功率上限下，某些波束的公平性阈值 `Λ_th`、容量匹配率阈值 `χ_th` 在物理上无法同时满足。这时优化问题无解怎么办？（软约束 + 罚函数？最小违约？） |
| D8 | 复杂度与实时性：SCA 每轮要解一个 SOCP/GP。Nb=100 时 cvxpy + ECOS/MOSEK 单次求解耗时？星上要在 Δt 内完成，Δt 是多少？这个瓶颈怎么论证"可工程落地"？ |
| D9 | 第三阶段是"每时隙重解"还是"事件触发重解"？如果每时隙重解，第三阶段就把第二阶段的 DRL 架空了（DRL 只提供初值）。两阶段的分工边界怎么划才不显得冗余？ |
| D10 | 开题报告创新点三说"以 DRL 结果为初始化，**显著降低优化求解成本**"。需要对照实验：随机初始化 / 均匀功率初始化 / T-SAC 初始化，三者的 SCA 迭代次数与最终目标值。这个实验打算怎么做？ |
| D11 | 题目要严格满足"公平性约束"，但公平性用 SGM 定义（式5-6）是**非凸非凹**的。要把它写成约束 `Λ_b ≥ Λ_th`，必须把 SGM 反解成 χ 和 Δ 的约束集合。SGM 的反函数是解析的吗？（涉及复数模 + 三次方） |
| D12 | 如果不反解 SGM，改用 Jain 公平指数或 max-min 公平，则与第二阶段的指标不统一，全文指标会割裂。怎么取舍？ |
| D13 | 第三阶段的"可解释性"主张怎么量化？建议：给出 KKT 条件/注水物理解释、对偶变量与干扰价格的对应关系、与 DRL 黑盒对照的可解释性案例。 |
| D14 | 第三阶段是否要做**在线/分布式**版本（如 ADMM 分解到各波束）？单星集中式与"星上算力受限"的叙事是否自洽？ |
| D15 | 中期答辩在 2026.09–10，第三阶段排在 2026.06–09。如果第三阶段只做"固定 X 优化 P"，工作量是否够一篇硕士学位论文的一章？要不要扩成"联合 X 松弛 + P 连续"？ |

## E. 三阶段耦合 / 框架层面的重构

| 编号 | 问题 | 关联证据 |
|---|---|---|
| E1 | 三阶段目前是**单向开环**：覆盖 → 频率功率 → 功率微调。第二阶段拿不到第三阶段的反馈。要不要做成**闭环迭代**（第三阶段的剩余违约量回传给第二阶段当奖励/课程信号）？ |
| E2 | 覆盖方案变化 → 干扰拓扑剧变 → 第二阶段策略失效。第一、二阶段的**时间尺度分离**（覆盖更新周期 Δt_cov vs 资源分配周期 Δt_res）有没有定量论证？开题报告只写了"每间隔更新一次"。 | 开题报告 2.1(二) |
| E3 | 三个阶段的**优化目标不统一**：第一阶段优化"覆盖率+负载均衡"，第二阶段优化"SGM+χ"，第三阶段优化"SINR+公平性"。整体系统级最优与三个局部最优的关系怎么论证（是否存在"局部对齐"的定理或至少单调性/一致性分析）？ |
| E4 | 第一阶段输出"波束指向+波束宽度"，第二阶段用"超级用户 = 波束指向中心"。但如果波束宽度可变（1.0°–2.2°，见 CSV），覆盖半径从约 312 km 变到 687 km，用单个中心点代表整个小区是否合理？误差怎么估？ |
| E5 | 第一阶段报告要"负载均衡"，但**负载的定义**是"波束覆盖区域内用户集合的总业务需求"。宽度变大 → 覆盖用户变多 → 负载变大，这会激励智能体**收缩波束**来降低负载，与"全覆盖"目标冲突。奖励里怎么平衡？有没有出现"退化解"？ |
| E6 | 覆盖满足率的定义里 "当用户处在波束覆盖范围内**且该用户未接入其他波束**时 `m_{u,b}=1`"——这是一个**先到先得**的分配规则，波束重叠区的用户归属依赖波束编号顺序。这公平吗？要不要改成 SINR 最大接入？ |
| E7 | 第一阶段计划用 ADS-B 航空业务数据；第二阶段用 `cover_output_list` 里的 rate；第三阶段链路级。三者的**业务模型是否一致**？同一条业务流的假设在三个阶段里能对齐吗？ |
| E8 | 开题报告说"缺乏地面业务分布数据 → 拟引入公开的地面移动通信业务统计数据"。具体数据源想好了吗（如 OpenCellID、Google/Meta 人口密度栅格、中国统计年鉴城镇化数据）？如何与 ADS-B 空域数据融合？ |
| E9 | 论文/报告反复强调"星上部署"，但当前所有实验都是**离线训练 + 地面仿真**。有没有算过推理延迟？要不要做一个 FP16/INT8 量化 + 推理时延的可行性小节？ |
| E10 | 代码仓库结构混乱（`old/` 下 12 个历史版本、`Environment_103.py` 与 `Environment_sac_p.py` 高度重复、`__pycache__` 混入）。重构建议：统一为 `env/ models/ algos/ baselines/ configs/ experiments/` + 一份配置驱动入口。要不要现在就做这个重构？ |
| E11 | 结果可复现性：目前没有固定 seed、没有 config 文件、没有实验记录（除 CSV log）。要不要引入 config 管理（Hydra/argparse + yaml）+ 每个实验一个 run 目录？ |
| E12 | 论文与代码的**指标口径**要对齐：SINR / CINR / SGM / satisfaction / capacity matching rate 五个指标在代码里各自怎么算、能不能一一对应到论文章节？建议做一张"指标-公式-代码位置"对照表。 |
| E13 | 三个阶段的**基线选择**：第一阶段 vs 聚类/MILP；第二阶段 vs MLP-SAC/CNN-SAC/DQN/PPO/贪心；第三阶段 vs 等功率/注水/纯 SCA。三组基线互不相同，最后的"系统级提升"要跟谁比？需要一个**端到端基线**（如"聚类覆盖 + 贪心频率 + 等功率"）。 |
| E14 | 中期答辩前的里程碑重排建议：把"覆盖算法实现"（原定 2026.01–03）压缩、把"统一仿真平台 + 指标对齐"前置。要不要重排时间表？ |
| E15 | 学位论文的**章节结构**是否按三阶段一章？还是按"问题建模 / 算法 / 优化 / 系统验证"四章？开题报告没写论文提纲。建议现在定下来，避免后期返工。 |

---

## 建议的提问顺序（若要高效推进）

1. **先诊断**：A2、A3、A12、A13、A10（奖励与约束的结构性缺陷）
2. **再定接口**：C1、C2、C3、E1、E2（三阶段怎么接起来）
3. **再定重构**：A9、A14、E10、E11、E12（工程重构方案）
4. **然后规划**：C13、C14、C15、D3、D4、D5、D6（二三阶段的实验与算法选型）
5. **最后评审**：B1–B15、E3、E13、E15（论文写作与答辩风险）
